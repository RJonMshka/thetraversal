---
summary: "Senior Software Engineer. Compiler nerd. Vedanta reader. Chicago."
---

The root node. Everything begins here — identity, projects, skills, philosophy, connection.

This portfolio is an Abstract Syntax Tree because every program is a tree, and a career is a program still being written. The visitor is the runtime. The traversal is the point.

Three layers mirror the Vedantic model: the landing (Brahman — undifferentiated source), the tree (Maya — projected structure), the node detail (Atman — the individual self navigating between the two). Not decorative. The medium is the message.
