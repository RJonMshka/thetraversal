---
summary: "Spring Boot, GraphQL, REST APIs, gRPC, Kafka, PostgreSQL, MongoDB, Redis."
---

The systems I reason about. Spring Boot microservices in production at Capital One — 15+ services, tuned resource configs, near-zero incidents. GraphQL as a data layer abstraction: migrated a financial application from REST to GraphQL at Publicis Sapient, reducing complexity and network call overhead. Kafka for event-driven pub/sub integration across the service mesh. PostgreSQL as the reliable truth store. MongoDB for document-oriented workloads. Redis for caching and session management. APIs as contracts between systems — the interface is the architecture.
